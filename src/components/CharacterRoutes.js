import React from 'react';

export default function CharacterRoutes() {
  return (
    <>
      <h4>Character Routes</h4>
      <p>
        This is where I will talk about the Character
        route endpoints, have a couple code block examples
        of the res.body, and maybe have a table with the endpoints.
      </p>
    </>
  );
}
//routes:
//get all characters
//get all characters searchable by name
//pagination for get all characters
//get random character
//get a random character by count
//get a character by id
